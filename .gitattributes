# Normalize line endings
* text=auto eol=lf

# Force TS source files to LF
*.ts text eol=lf

# dist bundle: force LF so Windows-built and Linux-built output is identical in git
dist/index.js text eol=lf

# Force binary for files that shouldn't be modified
*.png binary
*.jpg binary
*.gif binary
*.ico binary
